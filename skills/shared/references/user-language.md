# User-facing language

Detect the user's language from their messages and any host-provided language setting. Output all user-facing messages in that language.

When a template uses Korean as its reference format, translate it naturally while preserving its structure, formatting, and symbols. Do not mix languages within one message unless a literal command, identifier, path, or quoted value requires it.
